# Аудит Финанс — сайт (Next.js)

## Локальный запуск
```bash
npm i
npm run dev
```

## Деплой на Vercel
1) Импортируйте этот репозиторий в Vercel
2) Build Command: `next build` (по умолчанию)
3) Output: `.next` (по умолчанию)
